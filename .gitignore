# Python virtual environment
venv/
.venv/

# Python cache
__pycache__/
*.py[cod]
.pytest_cache/

# Node (if/when a JS frontend is added)
node_modules/

# Video files — reference clips and samples live outside version control
*.mp4
*.mov
*.avi
*.mkv
*.m4v
*.MP4
*.MOV

# Exception: the one short demo clip deployment/demo mode bundles and
# ships in the Docker image (see backend/demo.py) -- this is real,
# deployed content, not a training/reference asset.
!demo_assets/clip_whiff1.mkv

# Generated output
output/
uploads/
.cache/

# macOS
.DS_Store

# Editor
.vscode/
.idea/

# Environment / secrets
.env
