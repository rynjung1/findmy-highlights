{
  "clip": "clip_base3.mkv",
  "duration": 25.9,
  "notes": "New for Phase 10 prep, provided by the owner: third base throws to first, batter out. Not part of the original at-bat-focused reference set -- a short (~26s decodable), single-play clip. Annotated by frame-by-frame visual review (1fps contact sheet plus full-resolution frames around the play). This clip's exact offset/provenance within reference_clips/full_game.mkv is unconfirmed (a timestamp cross-check didn't line up, likely an offset mix-up at extraction time) -- not chased down further since the clip itself, not its derivation, is the ground truth source. DRAFT: owner review/correction pending before Phase 10 starts.",
  "events": [
    {"id": "e1", "window": [10, 18], "type": "hit_and_putout_first_base", "required": true,
     "check_continuity": true,
     "note": "batter loaded/swinging ~11; contact ~12-13 (bat already dropping near the plate, batter running toward first/right side, fielders converging near the mound/shortstop area by ~13); activity visible near the right-side base (the throw arriving / catch) ~16; defense relaxed and spreading back to normal positions by ~18, consistent with an out call resolving the play.",
     "phase11_resolution_marker_s": 17,
     "phase11_note": "Phase 11 acceptance criterion, NOT graded by Phase 10: play visually resolves (throw/catch activity at the base) at approximately t=16-17s, defense fully relaxed by ~18s -- using 17s as the single marker. Phase 11 should report how close the kept segment's end lands to this mark."}
  ],
  "base_occupancy": {
    "base": "first",
    "pixel_xy_approx": [1470, 555],
    "pixel_xy_note": "read visually off a full-resolution frame near the base bag; not a real calibration click -- a placeholder for validating compute_occupancy before the Phase 10 UI exists.",
    "occupied_window_s": [15, 17],
    "catch_tag_instant_s": 16.0,
    "note": "Phase 10 acceptance criterion. A fielder is visible in an active, crouched fielding/catching posture right at the base at ~16s, receiving the throw; defense relaxes back to normal positions by ~18. Precision +/-1s, same caveat as clip_base1."}
}
