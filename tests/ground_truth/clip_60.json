{
  "clip": "clip_60.mkv",
  "duration": 190.0,
  "notes": "Annotated by frame-by-frame visual review (contact sheets at 0.5-1 fps plus cropped 1 fps strips around each event). Times are approximate (±2s); event windows are sized to absorb that uncertainty. 'required: true' events are clearly real action and count toward recall; 'required: false' events are borderline (stance fidgeting, practice motions) — informational only, but per the never-miss rule the detector SHOULD usually catch them too. CORRECTED after owner review: t~26-49 was originally annotated as a swing (~33) plus hit-and-run (~40); the owner identified it as players throwing the ball around, and a wider-view re-check confirmed overhead throwing motions and casual jogging, not an at-bat.",
  "atbat_expectations": {
    "no_fire_within": [[137, 148]],
    "note": "the hit-and-run play window; the batter's frozen pre-pitch stance at ~137-139 must not read as a new at-bat"
  },
  "events": [
    {"id": "e1", "window": [0, 7], "type": "at_bat_activity", "required": false,
     "note": "clip opens mid-at-bat, batter in stance with movement; no confirmed swing"},
    {"id": "e2", "window": [26, 49], "type": "warmup_throws", "required": false,
     "note": "ball being thrown around near the plate area, overhead throws ~31-39, casual jogging ~40-45; NOT an at-bat (owner-corrected)"},
    {"id": "e4", "window": [120, 127], "type": "pitch", "required": true,
     "note": "ball visible near white-shirt batter ~122-124; no swing"},
    {"id": "e5", "window": [137, 148], "type": "hit_and_run", "required": true,
     "check_continuity": true,
     "note": "batter load/swing ~139-143, plate empty and fielders/runner moving by ~144; continuity-checked because Stage 3 play-extension depends on the signal holding through this defensive play"},
    {"id": "e6", "window": [168, 177], "type": "hit_and_run", "required": true,
     "note": "RECLASSIFIED 2026-08-14 (see docs/INVESTIGATION_LOG.md correction): originally logged as 'possible practice swings ~174' from a mound-region-only crop that excluded the batter, the rest of the field, and the ball. Full-frame review shows batter loaded with a complete defense positioned ~171.5-173.5, then the ball visible in flight above the infield with the pitcher running and the batter running to first ~174-175 -- a real at-bat with contact and a baserunning sequence, not a practice swing."}
  ]
}
