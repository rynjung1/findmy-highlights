{
  "clip": "clip_base4.mkv",
  "duration": 26.7,
  "notes": "New for Phase 10 prep, provided by the owner: third base throws to first, batter out (second such clip). Not part of the original at-bat-focused reference set -- a short (~27s decodable), single-play clip. Annotated by frame-by-frame visual review (1fps contact sheet plus full-resolution frames around the play). This clip's exact offset/provenance within reference_clips/full_game.mkv is unconfirmed (a timestamp cross-check didn't line up, likely an offset mix-up at extraction time) -- not chased down further since the clip itself, not its derivation, is the ground truth source. DRAFT: owner review/correction pending before Phase 10 starts.",
  "events": [
    {"id": "e1", "window": [11, 19], "type": "hit_and_putout_first_base", "required": true,
     "check_continuity": true,
     "note": "batter swinging ~12-13 (follow-through visible, bat already released); runner sprinting hard toward the right-side base and a second fielder also running by ~14, a more visibly urgent/close play than clip_base3; a fielder is standing at the right-side base with another player already jogging away past it by ~17, consistent with the throw beating the runner and an out call. Defense settling by ~19-20.",
     "phase11_resolution_marker_s": 17,
     "phase11_note": "Phase 11 acceptance criterion, NOT graded by Phase 10: play visually resolves (fielder holding the base, runner already jogging away past it) at approximately t=17s. Phase 11 should report how close the kept segment's end lands to this mark."}
  ],
  "base_occupancy": {
    "base": "first",
    "pixel_xy_approx": [1480, 555],
    "pixel_xy_note": "read visually off a full-resolution frame showing the base bag and fielder together; not a real calibration click -- a placeholder for validating compute_occupancy before the Phase 10 UI exists.",
    "occupied_window_s": [15, 18],
    "catch_tag_instant_s": 16.0,
    "note": "Phase 10 acceptance criterion. Fielder visible at the base receiving the throw by ~16s; a second player already jogging away past the base by ~17 confirms the play resolved by then. Precision +/-1s, same caveat as clip_base1/3."}
}
